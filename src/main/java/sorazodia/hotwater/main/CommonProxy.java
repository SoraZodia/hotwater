package sorazodia.hotwater.main;

import net.minecraft.block.Block;

public class CommonProxy
{
	/**
	 * Add mappings to the variables listed in fluid.json, but the 
	 * Block needs to implement IName.
	 * <br>
	 * Based on Biomes 'O Plenty's registerFluidBlockRendering() method
	 */
	public void registerBlockState(Block block)
	{
		
	}
}
